/**
 * agenda: make a function that prints a formatted string
 *
 * todo: [ ] count the number of charactes in the string,
 *       [ ] prints the one character
 *       [ ] prints the complete stream (no formatted string)
 *       [ ] take a char as an argument and converts it to a string
 */
#include "lib_.h"

int printf_(void) { return 0; }
